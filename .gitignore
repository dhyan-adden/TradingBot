node_modules/
.venv/
.pytest_cache/
__pycache__/
*.pyc
state/**/*.db
state/**/*.db-*
**/state/*.db
**/state/*.db-*
# archived engine-1 runtime state (kept on disk, not in git)
legacy/state/**/*.db
legacy/state/**/*.db-*
legacy/state/realtest/
.env
.DS_Store
.superpowers/
*.egg-info/

# rebuildable / transient scan artifacts (not audit trail)
tradeloop/config/universe_cache.json
tradeloop/_scan_archive/
tradeloop/reports/*.log

# runtime output - run directories and cycle logs
tradeloop/runs/
tradeloop/reports/cycle_logs/
tradeloop/reports/alerts.jsonl

# runtime state snapshots
tradeloop/state/live_book.json
tradeloop/state/archive/
